<!DOCTYPE html>
<html>
  <head>
    <%= page_title_tag %>

    <meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no, interactive-widget=resizes-content">
    <meta name="view-transition" content="same-origin">
    <meta name="color-scheme" content="light dark">
    <meta name="theme-color" content="#ffffff" media="(prefers-color-scheme: light)">
    <meta name="theme-color" content="#000000" media="(prefers-color-scheme: dark)">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= current_user_meta_tags %>

    <%= tag.meta name: "vapid-public-key", content: Rails.configuration.x.vapid.public_key %>

    <%= tag.link rel: "manifest", href: webmanifest_path(format: :json) %>
    <%= tag.link rel: "icon", href: fresh_account_logo_path, type: "image/png" %>
    <%= tag.link rel: "apple-touch-icon", href: fresh_account_logo_path %>
    <%= tag.link rel: "preconnect", href: "https://i.vimeocdn.com", crossorigin: "anonymous" %>
    <%= tag.link rel: "preconnect", href: "https://player.vimeo.com", crossorigin: "anonymous" %>

    <%= stylesheet_link_tag *(Stylesheets.from("application") + Stylesheets.vendor_stylesheets) %>
    <% if hotwire_native_app? %>
      <%= stylesheet_link_tag "native" %>
    <% end %>
    <%= stylesheet_link_tag "overrides" %>
    <%= custom_styles_tag %>

    <%= javascript_importmap_tags %>

    <%= yield :head %>
    
    <%= vite_react_refresh_tag %>
    <%= vite_client_tag %>
    <%= vite_typescript_tag "inertia" %>
    <%= inertia_ssr_head %>
    <%= vite_javascript_tag 'application' %>

  </head>

  <%# data-turbo="false" prevents Turbo Drive from intercepting links on Inertia pages %>
  <%# This avoids conflicts with Inertia's history management while keeping Stimulus active %>
  <body class="<%= body_classes %>" data-controller="local-time lightbox page-title" data-turbo="false">
    <a href="#main-content" class="skip-navigation btn">Skip to main content</a>
    <a href="#sidebar-toggle" class="skip-navigation btn">Skip to menu</a>

    <nav id="nav">
      <%= yield :nav %>
    </nav>

    <% if notice = flash[:notice] || flash[:alert] %>
      <div data-flash-container data-flash-<%= flash[:alert] ? "alert" : "notice" %>="<%= notice %>" style="display: none;"></div>
    <% end %>

    <main id="main-content" tabindex="-1">
      <%= yield %>

      <footer id="footer">
        <%= yield :footer %>
      </footer>
    </main>

    <template id="sidebar-slot" hidden>
      <%= yield :sidebar %>
    </template>

    <aside id="sidebar" data-controller="toggle-class" data-toggle-class-toggle-class="open" data-toggle-class-close-on-escape-value="true" data-toggle-class-focus-trap-value="true" data-toggle-class-initial-focus-selector-value="#room-search">
      <%= yield :sidebar %>
    </aside>

    <%= render "layouts/lightbox" %>

    <div id="toast-portal"></div>

    <a href="https://once.com" id="app-logo" target="_blank" aria-label="Once software from 37signals home page">
      <%= image_tag "campfire-icon.png", alt: "Campfire logo", width: 256, height: 216 %>
    </a>
  </body>
</html>

